{
  "name": "veil-bnb",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @veil/web",
    "build": "npm run build --workspace @veil/web",
    "typecheck": "npm run typecheck --workspace @veil/web",
    "agent": "npm run dev --workspace @veil/agent"
  },
  "engines": {
    "node": ">=20"
  }
}

